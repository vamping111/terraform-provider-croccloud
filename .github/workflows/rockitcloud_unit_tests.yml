name: Run tests and linters

on:
    pull_request:
      types: [ labeled, unlabeled, opened, synchronize ]

jobs:
    unit-tests:
        runs-on: ubuntu-22.04
        steps:
        - name: Setup Go
          uses: actions/setup-go@v2
          with:
            go-version: 1.21.x

        - uses: actions/checkout@v3
          with:
            ref: ${{ github.event.pull_request.head.sha }}

        - name: Install tools
          run: |
            make tools
        - name: Install Docker
          uses: crazy-max/ghaction-setup-docker@v3

        - name: Run Unit tests and linters
          if: "!contains(github.event.pull_request.labels.*.name, 'need_unit')"
          run: |
            make lint
            make test
            
        - name: Run documentation's linters
          if: "!contains(github.event.pull_request.labels.*.name, 'doc_linters')"
          run: |
            make docs-lint
            make website-lint
